@model Mission_08.Models.task

@{
    ViewData["Title"] = "Edit Task";
}

<h2 class="mb-4">Edit Task</h2>

<div class="row">
    <div class="col-md-6">
        <form asp-action="Edit" method="post">
            <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>
            <input asp-for="TaskId" type="hidden" />

            <div class="mb-3">
                <label asp-for="TaskName" class="form-label">Task <span class="text-danger">*</span></label>
                <input asp-for="TaskName" class="form-control" />
                <span asp-validation-for="TaskName" class="text-danger small"></span>
            </div>

            <div class="mb-3">
                <label asp-for="DueDate" class="form-label">Due Date</label>
                <input asp-for="DueDate" class="form-control" type="date" />
            </div>

            <div class="mb-3">
                <label asp-for="QuadrantId" class="form-label">Quadrant <span class="text-danger">*</span></label>
                <select asp-for="QuadrantId" class="form-select">
                    <option value="">-- Select Quadrant --</option>
                    <option value="1">Q1: Important / Urgent</option>
                    <option value="2">Q2: Important / Not Urgent</option>
                    <option value="3">Q3: Not Important / Urgent</option>
                    <option value="4">Q4: Not Important / Not Urgent</option>
                </select>
                <span asp-validation-for="QuadrantId" class="text-danger small"></span>
            </div>

            <div class="mb-3">
                <label asp-for="CategoryId" class="form-label">Category</label>
                <select asp-for="CategoryId" asp-items="ViewBag.Categories" class="form-select">
                    <option value="">-- Select Category --</option>
                </select>
            </div>

            <div class="mb-3 form-check">
                <input asp-for="Completed" class="form-check-input" type="checkbox" />
                <label asp-for="Completed" class="form-check-label">Mark as Completed</label>
            </div>

            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-warning">Save Changes</button>
                <a asp-action="Quadrants" asp-controller="Home" class="btn btn-outline-secondary">Cancel</a>
            </div>
        </form>
    </div>
</div>